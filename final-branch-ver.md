# final 브랜치 버전 관리

이 문서는 `final` 브랜치의 기준 버전, 배포 링크, 변경 이력, 운영 규칙을 기록합니다.

## 1. 브랜치 기준

- 브랜치: `final`
- 복제 기준: `gemini` 브랜치 최신 상태
- 기준 커밋: `14ce5c7`
- 기준 커밋 메시지: `feat: add intro preamble slide 'Dadok time'`
- 목적: 발표 최종본 관리 및 승인본 추적

## 2. 웹 링크

- gemini 기준 원본 링크: `https://presentationvibe-git-gemini-woowa0913s-projects.vercel.app`
- final 브랜치 링크: `https://presentationvibe-git-final-woowa0913s-projects.vercel.app`
- preview 배포는 푸시마다 갱신되므로 기록 기준은 branch alias 링크로 유지한다.
- 마지막 확인 시각: 2026-04-14 21:21 KST
- Vercel 프로젝트: `presentation_vibe`

## 3. 현재 상태

- `final` 브랜치는 현재 `gemini` 브랜치 내용을 기준으로 유지한다.
- 현재 차이는 `final` 전용 기록 문서, 좌측 하단 `final 브랜치` 라벨, 그리고 본문 슬라이드의 Stitch 레퍼런스 기반 전면 재구조화다.
- `intro`, 타이틀, 간지, `end` 페이지는 기존 구조와 톤을 유지한다.
- 본문 슬라이드는 기존 내용을 부분 보정하는 방식이 아니라, 레퍼런스 톤을 기본 디자인 시스템으로 삼아 카드/브라우저/에디토리얼 레이아웃으로 다시 설계했다.
- 라이트/다크는 의미 단위로 묶어 적용하고, 상단 프로그레스바는 디자인만 바꾸되 hover 시에만 보이도록 유지한다.
- 본문 슬라이드의 `Chapter` 칩은 좌측 인라인 방식이 아니라 우측 상단 영역으로 옮겨, hover 시 나타나는 상단 UI와 같은 맥락의 정보 레이어로 정리한다.
- 본문 타이틀은 칩과 분리해 더 왼쪽에서 시작하도록 정렬하고, 의미가 있는 3개 슬라이드에는 지속 모션 이펙트를 제한적으로 적용한다.
- 9페이지는 `gemini` 브랜치 9페이지의 2카드 구조를 기준으로 다시 정리하고, 블루 톤안과 원래 톤 비교안을 함께 유지한다.
- 2페이지는 여러 장의 분리된 장표처럼 넘어가는 구성이 아니라, 한 장 안에서 텍스트가 순차 등장하고 이동하는 상태형 애니메이션으로 관리한다.
- 원래 톤 비교안 슬라이드는 삭제했고, 발견 슬라이드는 좌측 설명 + 우측 4단계 이미지 전환 구조로 유지한다.
- 발견 슬라이드의 마지막 단계에서는 `[ 방대한 양, 알 수 없는 기호, 복잡한 언어 ] / 매번 'Hello World'에서 멈추던 과거...` 캡션이 나타난다.
- 직접 시연 슬라이드는 현재 12페이지에 배치되어 있으며, 상단 `Tiny Case` 브리프와 하단 브라우저형 검색 결과 프레임을 함께 사용한다.
- `Chapter 02 · Why Maker` 구간의 16~20페이지는 `gemini`/`stitch` 레퍼런스를 기반으로 구조를 다시 정렬해, 구조적 한계-역할 진화-방법론 전환-ADDIE 재발견 흐름을 더 큰 타이포와 설명 밀도로 재구성했다.
- `Chapter 04 · Case Study` 간지 뒤에는 밸런스 게임 라이브용 슬라이드가 추가되었고, 실제 밸런스 게임 앱의 로컬 빌드를 발표 안에서 바로 실행할 수 있게 구성했다.
- `Chapter 02`는 현재 `왜 HRD는 메이커가 되어야 하는가`에 집중하도록 문제 인식과 전환 브릿지 중심의 5장 구조로 정리되어 있다.
- `Chapter 03`는 `HRD의 바이브 코딩, 개념이 아니라 실행 방식`이라는 이름으로, 체험-정의-방법론-역량-수렴의 7장 구조로 재배치되어 있다.
- 최신 커밋 정보와 배포 상태는 작업 종료 시점의 `final` HEAD 기준으로 관리한다.
- `main` 브랜치는 변경하지 않는다.

## 4. 변경 이력

### v1.0.0 (2026-04-14) - final 브랜치 초기화

- `gemini` 브랜치 최신 상태를 `final` 기준으로 채택
- `final-branch-ver.md`를 공식 기록 문서로 생성
- 좌측 하단 브랜치 라벨을 `final 브랜치`로 정리

### v1.1.0 (2026-04-14) - 본문 디자인 리스타일링

- Stitch 레퍼런스 코드의 톤을 반영해 본문 슬라이드 전용 디자인 토큰과 카드/글래스 패널 스타일 적용
- `intro`, 타이틀, 간지, `end` 페이지는 유지하고 `ch1`~`ch6` 본문만 리스타일링
- 상단 네비게이션과 프로그레스바를 hover 기반으로 정리하고, 라이트/다크 UI가 현재 슬라이드 톤에 맞게 바뀌도록 개선
- `도파민 루프` 슬라이드를 겹침 없는 루프 카드 레이아웃으로 재구성
- 사례 허브, KAC 단계 카드, 온보딩 카드, 밸런스 게임 선택지 등 잘림 위험이 큰 본문 카드형 레이아웃 보정

### v1.2.0 (2026-04-14) - 본문 전면 재구조화

- 사용자 요청에 따라 "기존 내용 위에 스타일 일부 적용" 방식이 아니라, Stitch 레퍼런스 톤을 기본 화면 언어로 삼아 본문 슬라이드를 전면 재설계
- `ch3`~`ch6`를 포함한 본문 전 구간을 에디토리얼 카드, 브라우저 목업, KPI 보드, 비교 프레임, 툴킷/거버넌스 레이아웃으로 다시 구성
- 외부 iframe 중심의 사례 표현을 발표용 화면 구조로 정리하고, 실제 자산 이미지 기반의 정적인 시연 구도로 교체
- 다크 슬라이드 안의 light card 대비 문제와 flex shrink로 인한 카드 축소 이슈를 디자인 시스템 레벨에서 보정
- expanded fragment 기준 대표 슬라이드를 로컬 브라우저 캡처로 확인해 잘림과 대비 문제를 점검

### v1.2.1 (2026-04-14) - 본문 타이틀 정렬 및 모션 포인트 보강

- 본문 전 슬라이드의 `Chapter` 칩을 우측 상단으로 이동하고, 상단 hover UI가 나타날 때는 자연스럽게 물러나도록 정리
- 본문 타이틀 영역을 왼쪽 기준으로 다시 정렬해, 예시 슬라이드인 `그래서 만들어보려고 했습니다`를 포함한 전체 본문 타이틀의 시작점을 통일
- 지속 모션은 `바이브 코딩은 개발자 흉내가 아니라 실행 방식이다`, `KAC 사례의 임팩트`, `실행의 동력이 되는 도파민 루프` 3개 슬라이드에만 제한 적용
- effect toggle과 연동해 새 모션도 함께 끌 수 있도록 처리하고, 로컬 캡처로 hover 상태와 본문 레이아웃을 재점검

### v1.2.2 (2026-04-14) - p2 흐름 재구성 및 6페이지 gemini 기준 정렬

- p2를 4단계 상태 전환형 레이아웃으로 재구성해 `다음` 클릭 시 첨부 시안 흐름대로 장면이 바뀌도록 구성
- `다문`에 `多聞`을 반영하고, 마지막 단계에서는 상단 문장과 핵심 키워드 강조색을 적용
- 6페이지 `교육의 효과성을 높이고 싶었습니다`를 `gemini` 브랜치 6페이지와 같은 미니멀 다크 슬라이드 흐름으로 재구성
- p2 단계별 캡처와 6페이지 fragment 단계별 캡처로 시각 비교를 수행

### v1.2.3 (2026-04-14) - 8페이지 카드 구조 정렬

- 8페이지 `그래서 만들어보려고 했습니다`의 3개 카드를 공통 구조로 재정렬
- 각 카드를 `아이콘 - 제목 - 3줄 설명 - 하단 요약 배지` 순서로 통일해 설명 영역 높이와 여백 리듬을 맞춤
- 슬라이드 하단에 `어느 경로든 시간, 비용, 통제권 — 셋 중 하나는 포기해야 하는 경우가 많다` 문장을 별도 바닥 패널로 추가
- 로컬 캡처로 8페이지 전체 표시 상태와 overflow 0을 확인

### v1.2.4 (2026-04-14) - 9페이지 장벽 슬라이드 비교안 추가

- 9페이지 제목 문구를 `우리를`에서 `나를`로 조정
- `gemini` 브랜치 9페이지의 핵심 정보 구조를 기준으로, 2카드 중심의 미니멀 레이아웃으로 재구성
- 첫 번째 안은 블루 포인트 톤으로, 두 번째 안은 원래 웜 톤으로 구성해 바로 비교할 수 있도록 연속 슬라이드 2장으로 배치
- 하단 메시지도 `나를 두렵게 했던 것은 논리가 아니라 낯선 도구였습니다` 흐름으로 정리

### v1.3.0 (2026-04-14) - p2 단일 애니메이션화 및 사례 구간 재정렬

- 2페이지를 분리 장면형이 아니라 단일 슬라이드 내부 상태 전환형 애니메이션으로 다시 구성하고, 양 옆 배경 톤이 끊겨 보이던 문제를 viewport 단까지 확장해 정리
- 6페이지 다크 슬라이드도 같은 방식으로 viewport 배경을 동기화해 좌우 검은 띠와 본문 배경 차이가 나지 않도록 수정
- 8페이지 카드의 이모지 박스 장식을 제거하고, 요약 구조는 유지한 채 아이콘을 더 직접적으로 보이게 조정
- 본문 공통 폰트를 `WantedSans` 우선으로 재정렬하고, 9페이지 장벽 슬라이드 박스는 더 좁고 위쪽에 배치되도록 보정
- 원래 톤 비교안을 삭제하고, 발견 슬라이드는 `gemini` 10페이지를 참고한 좌측 설명 + 우측 4단계 이미지 전환 구조로 재편
- 직접 시연 슬라이드는 12페이지로 재배치하고, `Build Spec` / `What Changed`를 제거한 뒤 가로형 `Tiny Case` 브리프와 브라우저형 검색 결과 시연 화면으로 다시 구성

### v1.3.1 (2026-04-14) - 12페이지 검색 스냅샷 및 앱 토글 추가

- 12페이지 브라우저 프레임 하단을 실제 검색 결과 스냅샷처럼 보이는 Google 이미지 검색형 레이아웃으로 재구성
- 상단 주소창 우측에 `검색 화면` / `앱 실행` 버튼을 추가해 같은 프레임 안에서 상태를 전환할 수 있도록 구현
- `앱 실행` 클릭 시 `https://mac-shortcut-master-v4.vercel.app/` 를 iframe으로 로드해 레이아웃 안에서 바로 시연 가능하도록 연결
- 로컬 브라우저에서 검색 스냅샷 상태와 앱 실행 상태를 각각 캡처해 표시 및 전환 동작을 검증

### v1.3.2 (2026-04-14) - GEN 발표형 전환점/도파민/현장 장면 보정

- 11페이지 `작은 성공이 만든 전환점`을 `gemini` 발표 스타일에 맞춘 GEN형 풀카피 슬라이드로 재구성하고, `직접 만들 수 있다는 자신감`과 핵심 확신 문장은 기존 블루 포인트를 유지
- 11페이지의 `도파민이 터졌습니다.` 문장은 노란 강조 대신 옅은 회색 보조 문장으로 정리해 전체 리듬을 더 차분하게 맞춤
- 13페이지 하단 선언문은 배경 패널을 제거하고, 한 줄 블루 텍스트로 확대해 가독성을 높인 뒤 문구를 `"만들었다"는 작은 도파민이 자기주도적 문제해결을 이끕니다. 그렇다면 HRD 담당자가 왜 직접 이 방식으로 만들어야 하는가?` 로 교체
- 13페이지 마지막 장면에는 슬라이드 전역에 시차를 두고 반짝임이 터지는 sparkle 레이어를 추가하고, `effects-off` 토글과도 연동
- 15페이지 `현장에서 반복되는 장면`은 `gemini` 브랜치 15페이지의 GEN식 내러티브 구조를 참고해 대형 중앙 카피와 블루 강조 박스로 재구성

### v1.4.0 (2026-04-14) - Why Maker 구간 재구성 및 밸런스 게임 라이브 추가

- 16페이지 `기존 방식의 구조적 한계`는 `gemini` 16페이지 구조를 바탕으로 3행 내러티브 레이아웃으로 재구성하고, `경험 중심의 변화`는 현재 deck의 블루 포인트 컬러로 강조
- 17페이지 `전통적 HRD vs 지금 필요한 HRD`는 기존 final 비교 카드 구조를 유지하되, 카드 크기·텍스트 밀도·리스트 간격을 키워 여백이 덜 남도록 재조정
- 18페이지 `HRD는 어디로 진화하는가?`는 `stitch`/첨부 시안을 참고해 `Operator → Curator → Designer → Maker` 4단계 역할 카드와 하단 키 메시지 밴드 구조로 재설계
- 19페이지 `방법론의 전환`은 상단 키 메시지를 배경 없는 진한 블루 카피로 전환하고, 좌측 `전통적 ISD (ADDIE)` / 우측 `바이브코딩 x L&D` 패널의 높이와 리듬을 동일하게 정렬
- 20페이지 `AI-Augmented ADDIE`는 원래의 5카드 레이아웃을 유지한 채 각 단계에 블루 아이콘과 보조 설명을 추가하는 방향으로 정리하며, 중복 제목 줄과 이모지 아이콘은 제거한다.
- 18페이지 역할 진화 슬라이드는 상단 메시지를 배경 없는 블루 카피로 정리하고, 화살표는 다음 카드와 맞물려 등장하도록 fragment 타이밍을 연결한다.
- 19페이지 방법론 전환 슬라이드는 영문 레이블을 제거하고 한글 레이블만 남기며, 좌측 5개 단계와 우측 4개 단계의 전체 높이 합이 같도록 정렬한다.
- Chapter 03의 시작 장면은 25페이지에 `실시간 밸런스게임 — 여러분의 생각은?` 라이브 슬라이드를 추가하고, 외부 밸런스게임 앱을 맥 창 형태로 직접 임베드한다.
- 25페이지 밸런스게임 창은 가로폭을 약간 줄이고, 좌우·하단 여백을 같은 폭으로 맞추며, 주소 텍스트와 우측 안내 배지는 제거한 더 미니멀한 프레임으로 유지한다.
- Chapter 04 사례 구간에는 각 도구별 데모 링크를 연결하고, DISC/KAC 사례에는 전사 안내 메시지 이미지를 소개 화면으로 반영한다.
- `Chapter 04` 간지 바로 뒤에 밸런스 게임 라이브 슬라이드를 1장 추가하고, 맥 윈도우 스타일 프레임 안에서 앱을 실행할 수 있도록 구성
- 원본 `AI Studio` 앱 링크는 iframe 차단으로 직접 임베드가 불가능해, 로컬 `kt-cloud-balance-game` 빌드를 `assets/balance-game/` 아래에 포함하고 발표 안에서 바로 플레이되도록 대체
- 좌측 상단 교통신호 버튼 중 초록 버튼은 Fullscreen API에 연결해, 발표 중 해당 데모 창만 전체화면으로 확장 가능하게 구성

### v1.4.1 (2026-04-14) - 20페이지 ADDIE 카드 레이아웃 복원

- 20페이지 `AI-Augmented ADDIE: 모델의 재발견`을 리스트형 스택에서 다시 5카드 그리드 레이아웃으로 복원
- 중복으로 들어가 있던 보조 타이틀 줄은 제거해 제목 계층을 하나로 정리
- `🔎`, `🧩` 같은 이모지 아이콘은 모두 제거하고, 각 카드에는 블루 SVG 라인 아이콘을 새로 배치
- 기존 핵심 텍스트는 유지하되, 각 카드 하단에는 설명 카피를 덧붙여 의미 밀도를 보강
- `개발` 카드는 블루 강조 카드로 유지해 흐름의 중심축이 보이도록 정리

### v1.4.2 (2026-04-14) - 18~19페이지 타이밍 및 레이블 정리

- 18페이지 상단 메시지를 19페이지와 같은 배경 없는 블루 키 메시지 형태로 전환
- 18페이지의 `›` 화살표는 다음 카드와 같은 fragment index로 묶어, 카드 등장과 맞물려 순차 노출되도록 조정
- 19페이지 좌측 패널의 `Traditional ISD` 텍스트는 제거하고 `전통적 ISD (ADDIE)`만 남겨 정보 계층을 단순화
- 19페이지 우측 패널도 `Vibe Coding × L&D` 대신 `바이브코딩 x L&D`만 남기고 한글 중심 표현으로 정리
- 19페이지는 좌측 5개 단계와 우측 4개 단계의 총 높이 합이 같아지도록 카드 높이를 재조정

### v1.4.3 (2026-04-14) - Chapter 03 시작 밸런스게임 라이브 추가

- Chapter 03 간지 바로 뒤, 25페이지에 `실시간 밸런스게임 — 여러분의 생각은?` 슬라이드를 새로 추가
- `https://kt-cloud-balance-game.vercel.app/` 앱을 맥 윈도우 스타일 프레임 안에 직접 임베드
- 좌측 상단 맥 창 제어 버튼은 모두 새 창 실행 동작으로 연결해, 발표 중 외부 앱을 바로 띄울 수 있게 구성
- 상단에는 요청대로 타이틀만 두고, 하단은 앱 프레임 중심으로 단순하게 정리
- 공통 mac demo 로직을 보정해 fullscreen 버튼이 없는 슬라이드에서도 iframe 로드 후 화면이 정상 표시되도록 수정

### v1.4.4 (2026-04-14) - 25페이지 밸런스게임 프레임 정리

- 25페이지 밸런스게임 창의 가로폭을 살짝 줄여 슬라이드 안 여백 리듬을 더 안정적으로 조정
- 앱 프레임 테두리는 옅은 검정 계열로 분리해 배경과 자연스럽게 구분되도록 보정
- 창 높이를 더 길게 늘리고, 좌우·하단 여백 폭을 동일하게 맞춰 발표 화면에서 균형 있게 보이도록 수정
- 주소 텍스트와 우측 `좌측 상단 버튼 클릭 시 새 창 실행` 배지는 제거해 타이틀바를 단순화

### v1.4.5 (2026-04-14) - Chapter 04 사례 링크 및 소개 이미지 연결

- 사례 허브 카드에 KAC 서류 점검/모의고사, DISC, 칼리브레이션 데모 링크를 각각 연결
- `대표 사례 1` 장표의 제품 화면은 KAC 서류 점검/모의고사 안내 이미지를 2분할 프리뷰로 바꾸고, 두 데모 링크를 분리해 제공
- `대표 사례 2` 장표에는 DISC 전사 안내 이미지를 넣고, DISC 앱 데모 링크를 추가
- `대표 사례 3` 장표의 데모 링크는 Google Drive 기반 칼리브레이션 데모로 갱신
- 확장 사례 구간에는 `연봉 면담 데모` 링크를 추가해 추가 활용 사례도 바로 열 수 있게 연결

### v1.4.6 (2026-04-14) - Chapter 02/03 흐름 재배치

- `stich-v2` 흐름 기준으로 Chapter 02와 Chapter 03의 슬라이드 순서를 재배치
- Chapter 02는 `현장 반복 장면 → 구조적 한계 → 전통 vs 지금 → 진화 방향 → 패러다임 전환`으로 압축해 `왜`에 집중하도록 정리
- Chapter 03는 `밸런스게임 → 실행 방식 정의 → ISD에서 Agile L&D → AI-Augmented ADDIE → 5대 역량 → Maker DNA → HRD 메이커 수렴` 순서로 재구성
- Chapter 03 간지와 인덱스 표기를 `HRD의 바이브 코딩, 개념이 아니라 실행 방식`으로 통일

### v1.4.7 (2026-04-14) - 엔드 챕터 소개 장표 삭제

- 엔드 챕터의 `사실, 이 프레젠테이션도 / 바이브 코딩으로 만들었습니다.` 설명 슬라이드를 제거
- `사실, 이 프레젠테이션도` 예고 슬라이드 다음에 바로 Q&A 슬라이드로 이어지도록 정리

### v1.4.8 (2026-04-14) - 상단 UI 타이밍 및 본문 타이틀 선 정리

- 상단 네비게이션/프로그레스 UI는 마우스가 상단 영역을 벗어난 뒤 1초 내에 숨겨지도록 타이머를 단축
- 포인터가 창 밖으로 나가거나 탭이 비활성화된 경우에도 상단 UI가 남지 않도록 숨김 조건을 보강
- 본문 슬라이드 공통 배경에 있던 타이틀 아래 1px 구분선을 전체 제거

### v1.4.9 (2026-04-14) - 장표 위치/타이포 미세 조정

- 9페이지 장벽 슬라이드는 카드와 하단 문구를 화면 중앙보다 살짝 위에 오도록 재정렬
- 10페이지 발견 슬라이드는 `또 어려운 세계 이야기겠지` 문장을 3행으로 나누고, 4번째 이미지 단계의 하단 캡션을 더 위로 올려 가독성을 개선
- 15페이지 `현장에서 반복되는 장면`은 타이틀과 하단 강조 박스의 여백을 늘리고, 박스 폭과 폰트 크기를 키워 메시지 밀도를 완화
- 16페이지 `기존 방식의 구조적 한계`는 3개 요소 간 세로 간격을 넓혀 호흡감을 확보
- 18페이지 `HRD는 어디로 진화하는가`는 상단 키 메시지를 두 줄의 블루 카피로 바꾸고, `우리 조직에 맞는 도구를 직접 만드는 주체로 진화`만 강조하며 하단 카드들을 소폭 아래로 이동

### v1.5.0 (2026-04-14) - Chapter 04 KAC 사례 분리 및 라이브 데모 보강

- 29페이지 `Portfolio Snapshot` 카피를 `이 사례들의 공통점은 '멋진 앱을 만든 것'이 아니라, 학습과 변화의 병목을 직접 풀었다는 점입니다.` / `즉, 바이브 코딩은 기술 시연이 아니라 HRD의 실무 문제를 푸는 방식이었습니다.`로 교체
- 기존 결합 슬라이드였던 `대표 사례 1: KAC 서류 점검 → 모의고사`를 2장으로 분리해, 30페이지는 `서류 점검`, 31페이지는 `모의고사` 흐름으로 재구성
- 두 장 모두 좌측에는 실제 앱(`https://kac-doc-check.vercel.app/`, `https://kac-mock-test.vercel.app/`)을 iframe으로 직접 임베드하고, hover 시 우측 상단에 다크 톤 새 창 실행 버튼이 나타나도록 구성
- `Product View` 우측에는 같은 카드 헤더 라인 안에 `데모 열기` 버튼을 배치해 즉시 외부 실행이 가능하게 정리
- 우측 컬럼은 `Step 01/02` 설명 카드 + 실제 활용 스냅샷 + `Outcome/Shift/Effect` 구조로 재구성했고, 사용자가 제공한 전사 안내 이미지를 `Slack Snapshot` 증거 이미지로 활용
- KAC 허브 카드의 데모 링크도 새 앱 주소(`kac-doc-check`, `kac-mock-test`)로 갱신

### v1.5.1 (2026-04-14) - KAC 서류점검/모의고사 장표 발표형 보정

- 30/31페이지 좌상단 제목을 `대표 사례 1 : KAC 자격취득 지원 : 서류점검 -> 모의고사` 형식으로 통일하고, 각 슬라이드의 현재 단계(`서류점검`, `모의고사`)만 블루로 강조
- 30페이지는 `Outcome / Shift / Effect` 카드를 제거해 서류점검 앱과 실제 활용 스냅샷이 더 크게 보이도록 단순화
- 30페이지 좌측 앱 프레임은 세로 높이를 다시 확장해 시연 화면이 더 풍부하게 보이도록 조정
- 31페이지는 하단 `Outcome / Shift / Effect` 카드를 하단 밴드처럼 재배치해 잘리지 않도록 보정하고, 본문 카드와의 겹침을 줄이기 위해 앱/스냅샷 높이를 슬라이드별로 다르게 최적화

### v1.5.2 (2026-04-14) - 29페이지 사례 스냅샷 교체 및 높이 보정

- 29페이지 `HRD가 직접 만든 문제 해결 도구들`의 3개 사례 카드를 외부 링크 이미지 대신 `pptx-media` 기준 실제 PNG 스냅샷으로 교체
- KAC, DISC, 칼리브레이션 사례 이미지를 `case-kac-support.png`, `case-disc-dynamics.png`, `case-calibration-simulator.png`로 `final` 브랜치에 반영
- 사용자 요청에 맞춰 29페이지 카드 내 데모 링크는 모두 제거하고, 발표용 스냅샷 중심 레이아웃으로 단순화
- 29페이지 전용 카드/브라우저 높이와 본문 여백을 축소해 슬라이드 하단이 잘리지 않도록 보정하고 로컬 캡처로 전체 표시를 확인

### v1.5.3 (2026-04-14) - 30~34페이지 사례 장표 상호작용 보강

- 30페이지와 31페이지는 좌측 앱 프레임이 카드 하단까지 더 내려오도록 높이를 확장하고, 우측 `Slack Snapshot` 이미지는 클릭 시 확대 모달로 볼 수 있게 구성
- 31페이지 하단 `Outcome / Shift / Effect` 3카드는 제거해 앱과 실제 활용 스냅샷 중심의 발표형 레이아웃으로 단순화
- 32페이지 `KAC 사례의 임팩트` 상단/하단 카드 폭을 넓히고 설명 텍스트 크기를 소폭 줄여 줄바꿈을 최소화
- 34페이지는 브라우저 바의 `disc-coaching-analyzer.vercel.app` 주소 텍스트를 제거하고, 우측 상단에 `DISC 앱 데모` 버튼을 배치
- 34페이지 좌측 이미지는 클릭 시 확대 가능하게 하고, 슬라이드에서 `다음` 입력 시 해당 영역이 실제 `https://disc-coaching-analyzer.vercel.app/` 앱 iframe으로 전환되도록 상태형 인터랙션을 추가

### v1.5.4 (2026-04-14) - 34페이지 DISC 스냅샷 복원 및 앱 확대

- 34페이지 첫 화면의 좌측 이미지를 로컬 PNG가 아니라 이전에 사용하던 Google 링크 기반 슬랙 메시지 이미지로 복원
- 스냅샷 하단에 함께 보이던 앱 일부가 노출되지 않도록 원래 메시지 이미지 기준으로 정리
- `다음` 입력 후 노출되는 DISC 앱 iframe은 세로 높이를 더 키우고 화면 자체를 확대해 카드 안에서 더 꽉 차게 보이도록 조정

### v1.5.5 (2026-04-14) - 34페이지 DISC 앱 새 창 열기 버튼 추가

- 34페이지 DISC 앱 상태에서도 30페이지와 동일한 hover 기반 새 창 열기 버튼이 나타나도록 `case-app-float-open` 버튼을 추가
- 스냅샷 상태에서는 해당 버튼이 보이지 않도록 숨기고, 앱 상태에서만 hover 시 노출되게 정리

## 5. 운영 규칙

- 앞으로 모든 작업은 `final` 브랜치에서만 진행한다.
- 작업이 끝날 때마다 이 문서에 변경 사항을 기록한다.
- 작업이 끝날 때마다 `final` 브랜치에 커밋하고 푸시한다.
- 푸시 후 웹에서 볼 수 있는 링크를 함께 전달한다.
- `main` 브랜치는 건드리지 않는다.

## 6. 요구사항 기록

| 날짜 | 요청자 | 요구사항 |
|---|---|---|
| 2026-04-14 | user | `final` 브랜치를 만들고 현재 `gemini` 브랜치를 그대로 복사 |
| 2026-04-14 | user | 앞으로 `final-branch-ver.md`에 기록 |
| 2026-04-14 | user | 작업 후 `final` 브랜치에 커밋/푸시하고 웹 링크 제공 |
| 2026-04-14 | user | `main` 브랜치는 건드리지 않기 |
| 2026-04-14 | user | `intro`, 타이틀, 간지, `end`를 제외한 본문만 새 디자인 원칙으로 리스타일링 |
| 2026-04-14 | user | 본문에는 의미 단위의 라이트/다크 모드 적용, 잘림 없는 최적화, 프로그레스바는 hover 시에만 노출 |
| 2026-04-14 | user | 부분 적용이 아니라 레퍼런스 디자인 톤을 기본으로 본문을 완전히 재구조화 |
| 2026-04-14 | user | 본문 전체에서 `Chapter` 표기는 우측 상단으로 이동하고, 본문 타이틀은 더 왼쪽 정렬로 조정 |
| 2026-04-14 | user | 순차 등장 대신 의미 있는 슬라이드 3곳 정도에 지속 모션 이펙트 적용 |
| 2026-04-14 | user | p2는 첨부 이미지 흐름처럼 4단계 장면 전환으로 재구성하고 `다문`에는 한자 표기 추가 |
| 2026-04-14 | user | 6페이지는 `gemini` 브랜치 6페이지처럼 미니멀 다크 슬라이드 구성으로 조정 |
| 2026-04-14 | user | 8페이지는 카드 내부 설명 구조를 균일하게 맞추고, 바닥에 시간/비용/통제권 문장 추가 |
| 2026-04-14 | user | 9페이지는 `gemini` 브랜치 9페이지 구조를 기준으로 `우리`를 `나`로 바꾸고, 블루 톤안과 원래 톤안을 둘 다 만들어 비교 가능하게 유지 |
| 2026-04-14 | user | 2페이지와 6페이지는 좌우 배경 톤 차이가 없도록 viewport까지 배경을 확장하고, 2페이지는 한 장 안에서 텍스트가 등장/이동하는 애니메이션으로 재구성 |
| 2026-04-14 | user | 8페이지는 카드 안 이모지를 감싸는 작은 사각형을 제거 |
| 2026-04-14 | user | 9페이지는 Wanted 폰트 우선 적용 여부를 점검하고, 박스 폭을 줄이며 콘텐츠를 위쪽으로 올리기 |
| 2026-04-14 | user | 10페이지 비교안 삭제 |
| 2026-04-14 | user | 11페이지는 `gemini` 10페이지 구조 기반의 좌측 설명 + 우측 4단계 이미지 전환 슬라이드로 재구성 |
| 2026-04-14 | user | 12페이지는 `Tiny Case` 중심의 직접 시연형 레이아웃으로 바꾸고, 하단에 브라우저 주소창 포맷의 검색 시연 영역 추가 |
| 2026-04-14 | user | 12페이지 주소창 아래에는 첨부한 Google 이미지 검색형 화면을 반영하고, 버튼 클릭 시 `mac-shortcut-master-v4` 앱이 같은 레이아웃 안에 보이도록 토글 추가 |
| 2026-04-14 | user | 11페이지는 `gemini` 발표 스타일처럼 큰 GEN 카피로 바꾸고, 블루 강조는 유지하되 `도파민이 터졌습니다`는 옅은 회색으로 조정 |
| 2026-04-14 | user | 13페이지 하단 문구는 배경 없이 더 크게, 파란색 한 줄 선언으로 바꾸고 문구를 자기주도적 문제해결 관점으로 수정 |
| 2026-04-14 | user | 13페이지 마지막 애니메이션은 슬라이드 전역에 시차를 둔 반짝임 효과로 조정 |
| 2026-04-14 | user | 15페이지는 `gemini` 브랜치 15페이지 같은 GEN 발표 스타일로 바꾸고 색상은 블루 강조로 유지 |
| 2026-04-14 | user | 16페이지는 `gemini` 16페이지 구조로 바꾸고 `경험 중심의 변화`만 현재 박스 계열 블루 색상으로 강조 |
| 2026-04-14 | user | 17페이지는 현재 final 스타일을 유지하되 `gemini`처럼 더 크게 채워서 여백이 적게 보이도록 조정 |
| 2026-04-14 | user | 18페이지는 `stitch` 레퍼런스를 가져오되 텍스트를 전반적으로 키우고, 각 역할 설명을 첨부 시안 기준으로 추가 |
| 2026-04-14 | user | 19페이지는 상단 키 메시지를 배경 없이 진한 파란색 텍스트로, 좌측은 `전통적 ISD (ADDIE)`, 우측은 `바이브코딩 x L&D`로 정리하고 패널 요소 높이를 맞추기 |
| 2026-04-14 | user | 20페이지 각 단계 우측에 `stitch` 레퍼런스 아이콘을 넣고, 첨부 시안의 설명 문장을 카드 하단에 추가 |
| 2026-04-14 | user | 24페이지 간지 뒤에 밸런스 게임용 슬라이드를 1장 추가하고, `claude` 브랜치 15페이지 링크의 앱을 맥 창처럼 실행할 수 있게 구성 |
| 2026-04-14 | user | 좌측 상단 초록색 버튼을 누르면 데모 창만 전체화면으로 실행되게 처리 |
| 2026-04-14 | user | 20페이지는 원래 카드 레이아웃으로 되돌리고, 파란색 아이콘을 추가하되 이모지와 중복 제목은 삭제 |
| 2026-04-14 | user | 18페이지 화살표는 카드와 맞물려 등장하게 하고, 상단 키 메시지는 배경 없는 블루 텍스트로 수정 |
| 2026-04-14 | user | 19페이지는 영문 레이블을 제거하고 한글 레이블만 남기며, 좌우 단계 요소의 총 높이 합을 맞추기 |
| 2026-04-14 | user | Chapter 03 시작인 25페이지에 밸런스게임 라이브 슬라이드를 추가하고, 맥 창 제어 아이콘 클릭 시 외부 앱을 새 창으로 실행 |
| 2026-04-14 | user | 25페이지 밸런스게임 창은 폭을 조금 줄이고, 옅은 검정 테두리·동일한 좌우하단 여백·더 긴 세로 길이로 다듬고 주소/안내 문구는 제거 |
| 2026-04-14 | user | Chapter 04 사례 구간에 DISC, KAC, 칼리브레이션, 연봉 면담 데모 링크를 연결하고 DISC/KAC 소개 이미지를 반영 |
| 2026-04-14 | user | CH2/CH3 흐름을 `stich-v2`처럼 재배치하고, Chapter 03 네이밍을 `HRD의 바이브 코딩, 개념이 아니라 실행 방식`으로 수정 |
| 2026-04-14 | user | 엔드 챕터의 `사실, 이 프레젠테이션도 / 바이브 코딩으로 만들었습니다.` 장표 삭제 |
| 2026-04-14 | user | 상단 네비게이션은 1초 안에 사라지도록 줄이고, 본문 전체의 타이틀 아래 얇은 선 삭제 |
| 2026-04-14 | user | 9p, 10p, 15p, 16p, 18p의 위치/간격/강조 타이포를 발표 흐름에 맞게 미세 조정 |
| 2026-04-14 | user | 29페이지 스냅샷 문구를 교체하고, 30/31페이지 KAC 사례를 `서류점검`과 `모의고사`로 분리해 각각 라이브 앱, 실제 활용 스냅샷, Step 01/02 흐름으로 재구성 |
| 2026-04-14 | user | 30페이지는 `Outcome/Shift/Effect`를 제거하고 앱을 더 세로로 크게, 30/31페이지 제목은 공통 문장으로 통일하되 현재 단계만 블루로 강조, 31페이지 효과 카드는 아래로 내려 잘리지 않게 조정 |

## 7. 다음 업데이트 규칙

- 새 커밋이 생기면 `현재 상태`, `변경 이력`, `웹 링크`를 함께 갱신한다.
- 새 배포가 나오면 해당 링크와 시각을 이 문서에 반영한다.
- 사용자가 새 운영 원칙을 주면 `요구사항 기록` 표에 추가한다.
